# Habitat backoffice — environment configuration (constitution: Data Storage section)
# Copy to .env.local and fill in real values. Never commit .env.local.

NEXT_PUBLIC_SUPABASE_URL=https://nqvsotoqnbgyzjmsvfov.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service-role key: server-only (Edge Functions / test fixtures). NEVER exposed to the
# browser, never referenced from any client-bundled code path.
SUPABASE_SERVICE_ROLE_KEY=
